# ========================================
# Variables d'environnement Railway - Backend
# ========================================

# Database
# Railway va automatiquement fournir cette variable si vous liez PostgreSQL
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT & Security Secrets
# Générez avec: openssl rand -base64 32
JWT_SECRET=CHANGEZ_MOI_SECRET_FORT_32_CARACTERES
COOKIE_SECRET=CHANGEZ_MOI_SECRET_FORT_32_CARACTERES

# CORS Origins
# ⚠️ IMPORTANT : Remplacez par vos vraies URLs Railway après déploiement
STORE_CORS=https://votre-storefront-url.railway.app
ADMIN_CORS=https://votre-backend-url.railway.app
AUTH_CORS=https://votre-backend-url.railway.app

# Redis (Optionnel mais recommandé pour production)
# Si vous ajoutez Redis sur Railway, utilisez:
# REDIS_URL=${{Redis.REDIS_URL}}
# Sinon, laissez vide (utilisera fake redis en mémoire)
# REDIS_URL=

# Port (Railway définit automatiquement)
PORT=9000

# Node Environment
NODE_ENV=production

# Database name (optionnel)
DB_NAME=show-room-pro

# Admin Email (pour notifications, optionnel)
# ADMIN_EMAIL=admin@showroompro.com
